매개인자가 있는 함수는 반드시 실행할 때 매개인자의 값을 넣어줘야함.
	1. clearInterval -> setInterval을 삭제하는 함수
			setInterval의 아이디가 필요함

콜백함수 -> 실행식에 이름을 주고 실행 시 이름 호출해서 쓴다. 
	    매개인자가 있는 함수 (자동차 키)
	    앞뒤 버튼이 같은 함수 쓴다면? 매개인자로 구분하겠구나..!